<Window x:Class="ClaimsSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Claims System" Height="600" Width="800">
    <Grid>
        <!-- Main layout split into a navigation area and a content area -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Navigation Panel (Left Side) -->
        <StackPanel Grid.Column="0" Background="LightGray">
            <Button x:Name="LoginBtn" Content="Login" Margin="10" Height="30" />
            <Button x:Name="DashboardBtn" Content="Dashboard" Margin="10" Height="30" />
            <Button x:Name="NewClaimBtn" Content="New Claim" Margin="10" Height="30" />
            <Button x:Name="TrackClaimsBtn" Content="Track My Claims" Margin="10" Height="30" />
            <Button x:Name="ApproveClaimsBtn" Content="Approve Claims" Margin="10" Height="30" />
            <!-- Visibility of this button could be controlled by user role later -->
        </StackPanel>

        <!-- Content Area (Right Side) - This frame will host different pages -->
        <Frame x:Name="MainFrame" Grid.Column="1" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>